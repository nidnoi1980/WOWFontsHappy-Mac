name: Build macOS App

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'patches/**'
      - '.github/workflows/build-macos.yml'

permissions:
  contents: read

jobs:
  build-macos:
    runs-on: macos-latest
    timeout-minutes: 45

    steps:
      - name: Checkout port repository
        uses: actions/checkout@v4

      - name: Clone upstream v1.1 source
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p _upstream
          git clone --quiet --depth 1 --branch v1.1 \
            https://github.com/huchang47/WOWFontsHappy.git \
            _upstream/source
          echo "SRC_DIR=$GITHUB_WORKSPACE/_upstream/source" >> "$GITHUB_ENV"
          echo "Using upstream source: $GITHUB_WORKSPACE/_upstream/source"

      - name: Apply macOS port patches
        shell: bash
        run: |
          set -euo pipefail
          cd "$SRC_DIR"
          git apply "$GITHUB_WORKSPACE/patches/01-newfiles.patch"
          git apply "$GITHUB_WORKSPACE/patches/02-server.patch"
          git apply "$GITHUB_WORKSPACE/patches/03-ui.patch"
          git apply "$GITHUB_WORKSPACE/patches/04-package.patch"
          echo "Patches applied successfully."

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install application dependencies
        shell: bash
        run: |
          set -euo pipefail
          cd "$SRC_DIR"
          npm ci

      - name: Install Electron packaging tools
        shell: bash
        run: |
          set -euo pipefail
          cd "$SRC_DIR"
          npm install --no-save --package-lock=false electron@37.2.4 electron-builder@26.0.12

      - name: Syntax check
        shell: bash
        run: |
          cd "$SRC_DIR"
          npm run check

      - name: Run macOS path and API smoke test
        shell: bash
        run: |
          cd "$SRC_DIR"
          node .github/scripts/mac_smoke_test.js

      - name: Build unsigned Intel and Apple Silicon apps
        shell: bash
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
        run: |
          set -euo pipefail
          cd "$SRC_DIR"
          npx electron-builder --mac --x64 --arm64
          echo "Build output:"
          find dist-mac -maxdepth 2 -type f -print

      - name: Upload macOS installers
        uses: actions/upload-artifact@v4
        with:
          name: WoWFontsHappy-macOS-1.3.0
          path: |
            ${{ env.SRC_DIR }}/dist-mac/*.dmg
            ${{ env.SRC_DIR }}/dist-mac/*.zip
          if-no-files-found: error
          retention-days: 14
